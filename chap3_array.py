# min 과 max 를 활용하여 가장 큰 값과 가장 작은 값 찾기

'''
Java 에서는 Math.max 사용
python 에서는 max() 사용
'''

numbers=[3, 5, 1, 8, 2]

max_num = max(numbers)
print(f"가장 큰 수: {max_num}")

min_num = min(numbers)
print(f"가장 작은 수: {min_num}")